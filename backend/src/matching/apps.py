from django.apps import AppConfig


class MatchingConfig(AppConfig):
    """Додаток матчінгу: лайки, матчі, блоки, скарги."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'matching'
